backbone: clip
models:
  dinov2:
    hf_id: facebook/dinov2-small
    embedding_dim: 384
  clip:
    hf_id: openai/clip-vit-base-patch32
    embedding_dim: 512
device: auto
batch_size: 16
top_k_images: 50
top_k: 5
catalogue:
  dir: data/catalogue
  csv: data/catalogue/products.csv
index:
  dir: data/index_crop
preprocess:
  object_crop: true
